# USS Language Server moved

This standalone repository is no longer the canonical home for the USS language server.

Use the monorepo instead:

```text
https://github.com/GameBayoumy/zed-unity
```

Current source path:

```text
crates/uss-language-server
```

Current release assets are produced by the monorepo release workflow and downloaded by the Zed Unity extension.

This old repository should be treated as archived/read-only.
